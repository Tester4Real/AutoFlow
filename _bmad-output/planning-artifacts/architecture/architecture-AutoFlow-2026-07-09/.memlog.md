---
scope: AutoFlow Project Studio refactor
purpose: Build substrate for implementation
altitude: feature
updated: 2026-07-09T12:26
---

- (constraint) Brownfield runtime is Chrome Extension Manifest V3 2.2.21 with plain classic JavaScript, HTML, and CSS; no npm package, bundler, TypeScript, lint config, or automated test suite exists.
- (version) Chrome extension service workers support a single manifest background.service_worker file; classic importScripts() remains supported. Verified against Chrome for Developers on 2026-07-09.
- (version) Chrome sidePanel.default_path remains the manifest mechanism for a side panel page, and the API is listed for Chrome 114+. Verified against Chrome for Developers on 2026-07-09.
- (version) Chrome content scripts still distinguish MAIN and ISOLATED execution worlds; AutoFlow must preserve page fetch interception in MAIN and extension bridge/API work outside page-world logic.
- (decision) AD-1 Project is the top-level domain boundary. Binds storage, UI filtering, imports, variants, gallery, and queue state. Prevents global one-character/one-channel state from leaking across productions.
- (decision) AD-2 Stable IDs own identity for projects, assets, asset files, prompt records, image variants, video jobs, and Flow contexts. Binds all project domain records. Prevents display-name, alias, or filename edits from breaking relationships.
- (decision) AD-3 Use a two-surface UX architecture: Project Studio is a full extension page for project/asset/import/review/queue-building work; the existing side panel remains an operations cockpit. Prevents cramped large workflows and duplicated control ownership.
- (decision) AD-4 Required reference resolution gates image generation. Binds JSON import, resolver, asset uploads, and generation eligibility. Prevents prompts from generating while silently omitting listed character/place/style references.
- (decision) AD-5 Variant selection is metadata-first through prompt_id to variant_id mapping. Binds image review, gallery, downloads, and video draft preparation. Prevents destructive immediate renames and loss of alternate variants.
- (decision) AD-6 Video generation is manual queue work sourced from selected variants and animation prompts. Binds video draft builder, queue UI, and background video runtime. Prevents automatic image-completion-to-video execution.
- (decision) AD-7 Local project files and metadata are source of truth; Flow media/upload identifiers are cache entries scoped by Flow context. Binds asset files, selected image start frames, retry, sync, and account-switch recovery. Prevents stale IDs crossing Google accounts/projects.
- (decision) AD-8 Preserve ordered classic-script loaders. Binds background runtime, side panel app scripts, side panel CSS shards, and new Project Studio scripts/styles. Prevents bundler/module migration from becoming part of this refactor.
- (decision) AD-9 Project domain state mutates through one storage/reducer ownership layer per surface boundary. Binds project, asset, prompt, variant, selection, and job records. Prevents scattered chrome.storage writes and inconsistent derived state.
- (decision) AD-10 Preserve content-script and background boundaries: MAIN-world fetch wrapper observes Flow responses; isolated bridge forwards events; background runtime owns Chrome APIs, Flow API calls, downloads, and queue execution. Prevents extension API calls in page-world code and duplicated message routers.
- (decision) AD-11 Treat legacy Jack workflow as a migration boundary, not a universal reference. Binds import compatibility, bundled assets/reference/Jack.jpg, and future conversion work. Prevents hardcoded Jack assumptions from re-entering project-aware logic.
- (assumption) JSON without a references field remains valid and does not attach a hardcoded global character reference.
- (assumption) MVP video continuity uses selected image as start frame only; advanced video reference modes are deferred until Google Flow behavior is tested.
- (question) Open product decision remains: whether selecting a variant auto-creates a video draft or requires an explicit Create Video Draft action.
- (question) Open product decision remains: preferred JSON reference field name and normalization policy, such as references, refs, or both.
- (question) Open migration decision remains: automatic Jack import migration versus manual conversion into a normal Project Asset.
- (decision) Correction AD-5 Variant selection is metadata first through prompt_id to variant_id mapping. Binds image review gallery downloads video draft preparation. Prevents destructive immediate renames and loss of alternate variants.
- (decision) Correction AD-6 Video generation is manual queue work sourced from selected variants and animation prompts. Binds video draft builder queue UI background video runtime. Prevents automatic execution after image generation completes.
- (decision) Correction AD-7 Local project files and metadata are source of truth. Flow media and upload identifiers are cache entries scoped by Flow context. Binds asset files selected image start frames retry sync account switch recovery. Prevents stale identifiers crossing Google accounts or Flow projects.
- (decision) AD-12 Project persistence uses one versioned storage envelope autoflowProjectStateV1. Binds all project domain records schema migration sync repair shared reads. Prevents incompatible chrome storage keys and parallel project stores.
- (event) spine finalized
